import backend as backend  # noqa: F401


def load(file_path):
    """
    Creates the backend object from loading image in file path.

    Parameters
    ----------
    file_path : str
        File path to the image.

    Return
    ------
    backend.PaintingColors
        The backend object created using the image.
    """
    pass


def save(object, file_path="Untitled"):
    """
    Saves given backend object into given file.

    Parameters
    ----------
    object : backend.PaintingColors
        The backend object to turn into an image.

    file_path : str
        File path to save the image into.
    """
    pass
